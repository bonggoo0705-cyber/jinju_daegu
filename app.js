const weatherList=document.querySelector("#weather-list"),updatedAt=document.querySelector("#updated-at"),refreshButton=document.querySelector("#refresh-button");function parseCsv(t){const[e,...r]=t.trim().replace(/^\uFEFF/,"").split(/\r?\n/),a=e.split(",");return r.map(t=>Object.fromEntries(a.map((e,r)=>[e,t.split(",")[r]])))}function iconFor(t){return[0,1].includes(Number(t))?"☀️":[2,3,45,48].includes(Number(t))?"☁️":Number(t)>=71&&Number(t)<=86?"❄️":Number(t)>=95?"⛈️":"🌧️"}function formatTime(t){const e=new Date(t);return`${e.toLocaleDateString("ko-KR",{month:"long",day:"numeric"})} ${e.toLocaleTimeString("ko-KR",{hour:"2-digit",minute:"2-digit",hour12:!1})} 기준`}function weatherCard(t,e){const r=0===e?"#f6bd45":"#69b7d0";return`<article class="weather-card" style="--accent: ${r}"><div><p class="city">${t.location}</p><p class="condition">${t.weather_description}</p><p class="temperature">${t.temperature_2m}<small>°C</small></p></div><div class="weather-icon" aria-hidden="true">${iconFor(t.weather_code)}</div><div class="details"><span class="detail">체감 <strong>${t.apparent_temperature}°</strong></span><span class="detail">습도 <strong>${t.relative_humidity_2m}%</strong></span><span class="detail">강수 <strong>${t.precipitation} mm</strong></span></div></article>`}async function loadWeather(){weatherList.innerHTML='<p class="status">날씨 정보를 불러오는 중입니다.</p>';try{const t=await fetch(`data/weather.csv?updated=${Date.now()}`);if(!t.ok)throw new Error("CSV 파일을 찾을 수 없습니다.");const e=parseCsv(await t.text()).slice(-2);if(e.length<2)throw new Error("표시할 날씨 데이터가 부족합니다.");weatherList.innerHTML=e.map(weatherCard).join(""),updatedAt.textContent=formatTime(e[e.length-1].collected_at)}catch(t){weatherList.innerHTML=`<p class="error">날씨를 불러오지 못했어요.<br><small>${t.message}</small></p>`,updatedAt.textContent="data/weather.csv를 확인해 주세요."}}refreshButton.addEventListener("click",loadWeather),loadWeather();
